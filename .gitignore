# Build output
build/
app/build/
**/build/
dist/

# Gradle
.gradle/
# gradle-wrapper.jar is intentionally tracked — TAK.gov pipeline needs it to bootstrap
local.properties
*.local.properties

# Android Studio / IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
.DS_Store

# Signing / secrets — never commit these
*.jks
*.keystore
*.p12
*.pfx
*.pem
android_keystore
release.properties
signing.properties

# Environment / credential files
.env
.env.*
*.env
credentials.json
token.json
secrets.properties

# Generated source
app/src/main/gen/

# ProGuard mapping (keep if you need crash symbolication; gitignore if not)
app/build/proguard-civ-debug-mapping.txt

# OS junk
Thumbs.db
Desktop.ini
*.swp
*.swo
*~

# TAK devkit — local paths only
*.aar
!.takdev/aars/takdevlint.aar

# ATAK SDK main.jar — 31 MB, sourced from ATAK-CIV-SDK locally; not committed
main.jar
app/libs/main.jar

# Logs
*.log
